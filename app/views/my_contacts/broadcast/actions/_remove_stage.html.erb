<%
   # app/views/my_contacts/broadcast/actions/_stage_remove.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/stage_remove, locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<%= bootstrap_select_tag(
      field:   'user_action[remove_stage_id]',
      label:   { title: 'Stage' },
      options: { for_select: option_groups_from_collection_for_select(StageParent.where(client: presenter.client.id).includes(:stages), :stages, :name, :id, :name), blank: 'Select a Stage' }
    ) %>

<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
