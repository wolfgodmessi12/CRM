<%
   # app/views/my_contacts/broadcast/actions/_ok2text_on.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/ok2text_on', locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
