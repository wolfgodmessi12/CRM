<%
   # app/views/my_contacts/broadcast/actions/_add_tag.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/add_tag', locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<div id="contacts-add_tag">
   <%= render partial: 'tags/select_or_add', locals: { user: current_user, tag: presenter.client.tags.new, select_or_add_field_name: 'user_action[add_tag_id]', select_or_add_div_id: 'contacts-add_tag' } %>
</div>
<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
