<%
   # app/views/my_contacts/broadcast/actions/_add_group.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/add_group', locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<div id="contacts-group_add">
   <%= render partial: 'groups/select_or_add', locals: { user: current_user, group: presenter.client.groups.new, select_or_add_field_name: 'user_action[add_group_id]', select_or_add_div_id: 'contacts-group_add' } %>
</div>
<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
