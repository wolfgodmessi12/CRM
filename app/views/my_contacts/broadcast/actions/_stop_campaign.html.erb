<%
   # app/views/my_contacts/broadcast/actions/_stop_campaign.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/stop_campaign', locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<%= bootstrap_select_tag(
      field:   'user_action[stop_campaign_id]',
      label:   { title: 'Campaign' },
      options: { for_select: options_for_campaign(client: presenter.client, include_groups: true, add_options: [['All Campaigns', 'all']], grouped: true), blank: 'Select a Campaign' }
    ) %>

<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
