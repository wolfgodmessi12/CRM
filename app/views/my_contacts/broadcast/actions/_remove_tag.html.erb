<%
   # app/views/my_contacts/broadcast/actions/_remove_tag.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/remove_tag', locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<%= bootstrap_select_tag(
      field:   'user_action[remove_tag_id]',
      label:   { title: 'Tag' },
      options: { for_select: options_for_tag(presenter.client, '', [], false), blank: 'Select a Tag' }
    ) %>

<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
