<%
   # app/views/my_contacts/broadcast/actions/_remove_group.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/remove_group', locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<%= bootstrap_select_tag(
      field:   'user_action[remove_group_id]',
      label:   { title: 'Group' },
      options: { for_select: options_for_group(presenter.client, '', [], false), blank: 'Select a Group' }
    ) %>

<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
