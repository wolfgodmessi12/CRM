<%
   # app/views/my_contacts/broadcast/actions/_assign_lead_source.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/assign_lead_source', locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<%= bootstrap_select_tag(
      field:   'user_action[lead_source_id]',
      label:   { title: 'Lead Source' },
      options: { for_select: options_for_lead_source(presenter.client, []), blank: 'Select a Lead Source' }
    ) %>

<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
