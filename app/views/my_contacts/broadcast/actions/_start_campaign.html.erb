<%
   # app/views/my_contacts/broadcast/actions/_start_campaign.html.erb
   #
   # Example:
   #   render partial: 'my_contacts/broadcast/actions/start_campaign', locals: { presenter: MyContacts::Presenter }
   #
   # Instance Variables:
   #   none
   #
   # Local Variables:
   #   (req) presenter: (MyContacts::Presenter)
   #
   return unless local_assigns.dig(:presenter).is_a?(MyContacts::Presenter)
-%>
<%= bootstrap_select_tag_campaign(
      client:  presenter.client,
      field:   'user_action[apply_campaign_id]',
      label:   { title: 'Campaign' },
      options: { active_only: true }
    ) %>

<%= render partial: 'my_contacts/broadcast/actions/show_calendar', locals: { presenter: } %>
<%= render partial: 'my_contacts/broadcast/actions/show_processing', locals: { presenter: } %>

<script>
  $(function() {
    updateTimeToComplete();
  });
</script>
