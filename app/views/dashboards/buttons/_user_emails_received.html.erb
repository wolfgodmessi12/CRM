<%
  # app/views/dashboards/buttons/_user_emails_received.html.erb
  #
  # Example:
  #   render partial: 'dashboards/buttons/user_emails_received', locals: { presenter: DashboardPresenter }
  #
  # Instance Variables:
  #   none
  #
  # Local Variables:
  #   (req) presenter: (DashboardPresenter)
  #
  return unless local_assigns.dig(:presenter).is_a?(DashboardPresenter)
-%>
<%= render partial: 'dashboards/dash_button', locals: {
      id:               'user_emails_received',
      stats:            Messages::Message.emails_received_by_user(presenter.user_settings_buttons.data[:buttons_user_id], presenter.date_range[0], presenter.date_range[1]).count.to_s,
      description:      'Emails Received',
      description_icon: 'u',
      icon:             'fa fa-envelope',
      link:             presenter.client.my_contacts_allowed ? my_contacts_path : '#',
      chart_id:         '',
      chart_value:      0,
      button_movable:   true
    } %>
