<%
  # app/views/triggeractions/forms/_355.html.erb
  #
  # Example:
  # 	render partial: "triggeractions/forms/355", locals: { presenter: Campaigns::Presenter }
  #
  # Instance Variables:
  #   none
  #
  # Local Variables:
  #   (req) presenter: (Campaigns::Presenter)
  #
  # remove from a Group
  #
  return unless local_assigns.dig(:presenter).is_a?(Campaigns::Presenter) && presenter.triggeraction.is_a?(Triggeraction)
-%>
<%= bootstrap_select_tag(
      field:        'triggeraction[group_id]',
      label:        { title: 'Group' },
      html_options: { disabled: presenter.campaign_disabled? },
      options:      { for_select: options_for_group(presenter.client, presenter.triggeraction.group_id, [], false), blank: 'Select a Group' }
    ) %>
