<%
  # app/views/campaigns/index/starting_this_campaign/_servicetitan.html.erb
  #
  # Example:
  #   render partial: 'campaigns/index/starting_this_campaign/servicetitan', locals: { presenter: Campaigns::Presenter }
  #
  # Instance Variables:
  #   none
  #
  # Local Variables:
  #   (req) presenter: (Campaigns::Presenter)
  #
  return unless local_assigns.dig(:presenter).is_a?(Campaigns::Presenter)
-%>
<% if presenter.servicetitan_events_starting_this_campaign(presenter.campaign.id).present? %>
  <%= link_to "<i>#{image_tag('integrations/servicetitan/logo.png', { style: 'height:20px;' })}</i>".html_safe,
              integrations_servicetitan_path(card: 'events'),
              class: 'btn btn-sm btn-icon btn-secondary' %>
<% end %>
