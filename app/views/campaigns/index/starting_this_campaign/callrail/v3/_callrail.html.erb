<%
  # app/views/campaigns/index/starting_this_campaign/callrail/v3/_callrail.html.erb
  #
  # Example:
  #   render partial: 'campaigns/index/starting_this_campaign/callrail/v3/callrail', locals: { presenter: Campaigns::Presenter }
  #
  # Instance Variables:
  #   none
  #
  # Local Variables:
  #   (req) presenter: (Campaigns::Presenter)
  #
  return unless local_assigns.dig(:presenter).is_a?(Campaigns::Presenter)
-%>
<% if presenter.callrail_events_starting_this_campaign(presenter.campaign.id).present? %>
  <%= link_to "<i>#{image_tag('integrations/callrail/logo-mini.svg', { style: 'height:20px;' })}</i>".html_safe,
              integrations_callrail_v3_path(card: 'events'),
              class: 'btn btn-sm btn-icon btn-secondary' %>
<% end %>
