<%
  # app/views/campaigns/index/starting_this_campaign/_housecallpro.html.erb
  #
  # Example:
  #   render partial: 'campaigns/index/starting_this_campaign/housecallpro', locals: { presenter: Campaigns::Presenter }
  #
  # Instance Variables:
  #   none
  #
  # Local Variables:
  #   (req) presenter: (Campaigns::Presenter)
  #
  return unless local_assigns.dig(:presenter).is_a?(Campaigns::Presenter)
-%>
<% if presenter.housecallpro_events_starting_this_campaign(presenter.campaign.id).present? %>
  <%= link_to "<i>#{image_tag('integrations/housecallpro/logo_black.svg', { style: 'height:20px;' })}</i>".html_safe,
              integrations_housecall_path(card: 'webhooks'),
              class: 'btn btn-sm btn-icon btn-secondary' %>
<% end %>
