<%
  # app/views/campaigns/_analyze_icon_with_name.html.erb
  #
  # Example:
  #   render partial: 'campaigns/analyze_icon_with_name', locals: { campaign: Campaign }
  #
  # Instance Variables:
  #   none
  #
  # Local Variables:
  #   (req) campaign: (Campaign)
  #
  return unless local_assigns.dig(:campaign).is_a?(Campaign)
-%>
<%= render partial: 'campaigns/analyze_icon', locals: { campaign: } %>
<%= campaign.name -%>
