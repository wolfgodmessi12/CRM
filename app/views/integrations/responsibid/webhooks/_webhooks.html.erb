<%
  # app/views/integrations/responsibid/webhooks/_webhooks.html.erb
  #
  # Example:
  #   render partial: 'integrations/responsibid/webhooks/webhooks', collection: @client_api_integration.webhooks, as: :webhooks, locals: { presenter: Presenter }
  #
  # Required Instance Variables:
  #   none
  #
  # Required Local Variables:
  #   none
  #
  presenter = local_assigns.dig(:presenter) || Integrations::Responsibid::Presenter.new(client_api_integration: @client_api_integration)
-%>
<%= render partial: 'integrations/responsibid/webhooks/event_outer', collection: webhooks.last, as: :webhook_event, locals: { presenter: presenter } %>
