<%
  # app/views/affiliates/reports/cardx/_client.html.erb
  #
  # Example:
  #   render partial: 'affiliates/reports/cardx/client', collection: @clients.order(:name), as: :client, locals: { presenter: }
  #
  # Instance Variables:
  #   (req) client: (Client)
  #
  # Local Variables:
  #   (req) presenter: (Presenter)
  #
  return unless local_assigns.dig(:presenter).is_a?(Affiliates::Presenter)
-%>
<tr>
  <td colspan="2" class="text-left align-middle"> <%= client.name -%> </td>
  <td class="text-right align-middle"> <%= number_to_currency(client.amount_requested) -%> </td>
  <td class="text-right align-middle"> <%= number_to_currency(client.amount_fees) -%> </td>
  <td class="text-right align-middle"> <%= number_to_currency(client.amount_total) -%> </td>
</tr>
