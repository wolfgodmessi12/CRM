<%
  # app/views/affiliates/reports/cardx/printable.html.erb
  #
  # Example:
  #   render 'affiliates/reports/cardx/printable'
  #
  # Instance Variables:
  #   (opt) @clients:       (Clients)
  #
  # Local Variables:
  #   none
  #
  presenter = Affiliates::Presenter.new(affiliate: @affiliate)
-%>
<div class="p-5">
  <h4><i class="fa fa-handshake mr-2"></i>Affiliate Report</h4>
  <p>
    Affiliate: CardX<br>
    Period: <%= @report_period_string -%><br>
    Printed: <%= Time.current.in_time_zone(current_user.client.time_zone).strftime('%m/%d/%Y %H:%M %p') -%>
  </p>

  <%= render partial: 'affiliates/reports/cardx/results', locals: { presenter: } %>
</div>
